export interface HeroTitleProps {
  title: string;
  id: string;
}
export interface VideoState {
  isEnd: boolean;
  startPlay: boolean;
  videoId: number;
  isLastVideo: boolean;
  isPlaying: boolean;
}
